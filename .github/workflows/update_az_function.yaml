name: Update Azure Function App Setting

on:
  workflow_run:
    workflows: ["Deploy Azure Function Infrastructure"]
    types:
      - completed

  push:
    branches:
      - dev
      - feature1
    paths:
      - '.github/workflows/update_az_function.yaml'

  workflow_dispatch:

jobs:
  update_function_app_setting:
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name != 'workflow_run' }}
    runs-on: ubuntu-latest

    env:
      RESOURCE_GROUP_NAME: healthcare_project
      FUNCTION_APP_NAME: process-file-func

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Setup Terraform CLI
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.7

      - name: Terraform Init
        working-directory: terraform/dev/infra
        run: terraform init

      - name: Terraform Get Storage Account Name
        id: get_storage_name
        working-directory: terraform/dev/infra
        run: |
          STORAGE_ACCOUNT_NAME=$(terraform output -raw storage_account_name)
          echo "storage_account_name=$STORAGE_ACCOUNT_NAME" >> $GITHUB_OUTPUT

      - name: Get Storage Account Key
        id: get_storage_key
        run: |
          STORAGE_KEY=$(az storage account keys list \
            --resource-group ${{ env.RESOURCE_GROUP_NAME }} \
            --account-name ${{ steps.get_storage_name.outputs.storage_account_name }} \
            --query '[0].value' -o tsv)
          echo "storage_key=$STORAGE_KEY" >> $GITHUB_OUTPUT
     
      # Step  Infrastructure

      - name: Update Function App Setting STORAGE_CONN
        run: |
          STORAGE_CONN="DefaultEndpointsProtocol=https;AccountName=${{ steps.get_storage_name.outputs.storage_account_name }};AccountKey=${{ steps.get_storage_key.outputs.storage_key }};EndpointSuffix=core.windows.net"

          az functionapp config appsettings set \
            --name ${{ env.FUNCTION_APP_NAME }} \
            --resource-group ${{ env.RESOURCE_GROUP_NAME }} \
            --settings STORAGE_CONN="$STORAGE_CONN"
